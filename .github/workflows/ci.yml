name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  DOCKER_REGISTRY: docker.io
  IMAGE_NAME: ${{ secrets.DOCKER_USERNAME }}/laravel-app

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: password
          MYSQL_DATABASE: laravel_test
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3
        ports:
          - 3306:3306

    steps:
    - uses: actions/checkout@v3
    
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: mbstring, dom, fileinfo, mysql
        coverage: xdebug

    - name: Copy .env
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"
      working-directory: ./src

    - name: Install Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
      working-directory: ./src

    - name: Generate key
      run: php artisan key:generate
      working-directory: ./src

    - name: Directory Permissions
      run: chmod -R 777 storage bootstrap/cache
      working-directory: ./src

    - name: Run Tests
      run: php artisan test
      working-directory: ./src
      env:
        DB_CONNECTION: mysql
        DB_HOST: 127.0.0.1
        DB_PORT: 3306
        DB_DATABASE: laravel_test
        DB_USERNAME: root
        DB_PASSWORD: password

    - name: Run PHPStan
      run: ./vendor/bin/phpstan analyse
      working-directory: ./src

    - name: Run Laravel Pint
      run: ./vendor/bin/pint --test
      working-directory: ./src

  build:
    needs: test
    runs-on: ubuntu-latest
    # Only build and push images when pushing to main or develop branches
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v4
      with:
        images: ${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=sha,prefix={{branch}}-
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./docker/php/Dockerfile
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache
        cache-to: type=registry,ref=${{ env.IMAGE_NAME }}:buildcache,mode=max
        build-args: |
          APP_ENV=${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }}

#   deploy-production:
#     needs: build
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/main'
#     environment: production
    
#     steps:
#     - name: Deploy to Production Server
#       uses: appleboy/ssh-action@v0.1.5
#       with:
#         host: ${{ secrets.EC2_PRODUCTION_HOST }}
#         username: ${{ secrets.EC2_USERNAME }}
#         key: ${{ secrets.EC2_SSH_KEY }}
#         script: |
#           # Pull latest production image
#           docker pull ${{ secrets.DOCKER_USERNAME }}/laravel-app:main
          
#           # Stop existing container
#           docker stop laravel-app-production || true
#           docker rm laravel-app-production || true
          
#           # Run new container
#           docker run -d \
#             --name laravel-app-production \
#             --restart unless-stopped \
#             -p 80:80 \
#             -v /home/ubuntu/laravel-storage:/var/www/storage \
#             -v /home/ubuntu/.env.production:/var/www/.env \
#             --network laravel-network \
#             ${{ secrets.DOCKER_USERNAME }}/laravel-app:main
          
#           # Run migrations
#           docker exec laravel-app-production php artisan migrate --force
          
#           # Clear and cache configs
#           docker exec laravel-app-production php artisan config:cache
#           docker exec laravel-app-production php artisan route:cache
#           docker exec laravel-app-production php artisan view:cache
          
#           # Health check
#           sleep 10
#           curl -f http://localhost/health || exit 1
    
#     - name: Notify Slack
#       uses: 8398a7/action-slack@v3
#       if: always()
#       with:
#         status: ${{ job.status }}
#         text: "Production deployment ${{ job.status }}"
#         webhook_url: ${{ secrets.SLACK_WEBHOOK }}